game class do 
	//atributes 
	set gameBoard = null
	set gameBoardCount = 0
	procedure start do 
		while hasNextInt do 
			input rowCount,colCount	
			gameBoard := new GameBoard(row, count)
			if(gameBoard.validate()) do 
				gameBoard.searchFigures()
			end
			else do 
				output invalid terrain
			end
		end
	end
end